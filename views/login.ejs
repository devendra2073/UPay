<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>UPay - Login & Registration</title>
    <script src="/js/google.js" defer=""></script>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/npm/toastify-js/src/toastify.min.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/toastify-js/1.6.1/toastify.js"></script>
    <script src="https://www.google.com/recaptcha/api.js" async defer></script>
    
    <style>
        .fade-in { animation: fadeIn 0.4s ease-in-out; }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .hidden-form { display: none; }
        
        .spinner {
            border: 2px solid rgba(255, 255, 255, 0.3);
            border-radius: 50%;
            border-top: 2px solid #fff;
            width: 16px;
            height: 16px;
            animation: spin 1s linear infinite;
            display: inline-block;
            margin-right: 8px;
        }
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
    </style>
</head>
<body class="bg-gradient-to-br from-slate-50 to-indigo-100 min-h-screen flex items-center justify-center p-6 font-sans">

    <div class="max-w-md w-full">
        <div class="text-center mb-8">
            <div class="inline-block p-3  mb-2">
                <img src="/upay.png" alt="upay" class="h-15 object-contain"/>
            </div>
            <h1 class="text-2xl font-bold text-slate-800" id="page-title">Welcome Back</h1>
            <p class="text-slate-500 mt-1" id="page-subtitle">Please enter your details to sign in.</p>
        </div>

        <div class="bg-white rounded-3xl shadow-xl p-8 border border-slate-100">
            
            <form id="login-form" class="space-y-5 fade-in">
                <div>
                    <label class="block text-sm font-semibold text-slate-700 mb-1">Email Address</label>
                    <input type="email" name="email" required placeholder="name@company.com" class="w-full px-4 py-3 rounded-xl border border-slate-200 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 outline-none transition-all">
                </div>
                <div>
                    <label class="block text-sm font-semibold text-slate-700 mb-1">Password</label>
                    <input type="password" name="password" required placeholder="••••••••" class="w-full px-4 py-3 rounded-xl border border-slate-200 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 outline-none transition-all">
                </div>
                
                <div class="flex justify-center py-2">
                    <div class="g-recaptcha" data-sitekey="6Lcaq4csAAAAAJwVD8N3_NP8ToFWYBnrU7baLOnc"></div>
                </div>

                <div class="flex items-center justify-between text-sm">
                    <label class="flex items-center text-slate-600 cursor-pointer">
                        <input type="checkbox" name="remember" class="mr-2 rounded border-gray-300 text-indigo-600"> Remember me
                    </label>
                    <a href="/user/forgot-password" class="text-indigo-600 font-semibold hover:text-indigo-700">Forgot password?</a>
                </div>
                <button type="submit" class="w-full bg-indigo-600 text-white font-bold py-3.5 rounded-xl hover:bg-indigo-700 transition duration-300 shadow-lg shadow-indigo-200 active:scale-[0.98] flex items-center justify-center">
                    <span>Sign In</span>
                </button>
            </form>

            <form id="register-form" class="space-y-4 fade-in hidden-form">
                <div class="grid grid-cols-2 gap-4">
                    <div>
                        <label class="block text-sm font-semibold text-slate-700 mb-1">First Name</label>
                        <input type="text" name="firstname" required placeholder="John" class="w-full px-4 py-3 rounded-xl border border-slate-200 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 outline-none transition-all">
                    </div>
                    <div>
                        <label class="block text-sm font-semibold text-slate-700 mb-1">Last Name</label>
                        <input type="text" name="lastname" required placeholder="Doe" class="w-full px-4 py-3 rounded-xl border border-slate-200 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 outline-none transition-all">
                    </div>
                </div>
                <div>
                    <label class="block text-sm font-semibold text-slate-700 mb-1">Email Address</label>
                    <input type="email" name="email" required placeholder="name@company.com" class="w-full px-4 py-3 rounded-xl border border-slate-200 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 outline-none transition-all">
                </div>
                <div>
                    <label class="block text-sm font-semibold text-slate-700 mb-1">Create Password</label>
                    <input type="password" name="password" required placeholder="••••••••" class="w-full px-4 py-3 rounded-xl border border-slate-200 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 outline-none transition-all">
                </div>

                <div class="flex justify-center py-2">
                    <div class="g-recaptcha" data-sitekey="6Lcaq4csAAAAAJwVD8N3_NP8ToFWYBnrU7baLOnc"></div>
                </div>

                <button type="submit" class="w-full bg-indigo-600 text-white font-bold py-3.5 rounded-xl hover:bg-indigo-700 transition duration-300 shadow-lg shadow-indigo-200 mt-2 flex items-center justify-center">
                    <span>Create Account</span>
                </button>
            </form>

            <div class="relative my-8">
                <div class="absolute inset-0 flex items-center"><span class="w-full border-t border-slate-100"></span></div>
                <div class="relative flex justify-center text-xs uppercase">
                    <span class="bg-white px-4 text-slate-400 tracking-widest font-medium">Or continue with</span>
                </div>
            </div>

        
                <div id="buttonDiv"></div>
               
            
        </div>

        <p class="text-center mt-8 text-slate-500 font-medium">
            <span id="toggle-text">Don't have an account?</span>
            <button id="toggle-btn" class="text-indigo-600 font-bold hover:underline ml-1 cursor-pointer">Sign up</button>
        </p>
    </div>
    <script>
        function tst(text, type = 'success') {    
            Toastify({
                text: text,
                duration: 3000,
                close: true,
                gravity: "top",
                position: "right",
                style: {
                    background: type === 'success' ? "#4f46e5" : "#ef4444",
                    borderRadius: "12px",
                }
            }).showToast();
            navigator.vibrate(300)
        }

        const loginForm = document.getElementById('login-form');
        const registerForm = document.getElementById('register-form');
        const toggleBtn = document.getElementById('toggle-btn');
        const toggleText = document.getElementById('toggle-text');
        const pageTitle = document.getElementById('page-title');
        const pageSubtitle = document.getElementById('page-subtitle');

        let isLogin = true;

        toggleBtn.addEventListener('click', (e) => {
            e.preventDefault();
            isLogin = !isLogin;
            loginForm.classList.toggle('hidden-form');
            registerForm.classList.toggle('hidden-form');
            grecaptcha.reset(); // Reset captcha when switching forms
            
            if (isLogin) {
                pageTitle.innerText = "Welcome Back";
                pageSubtitle.innerText = "Please enter your details to sign in.";
                toggleText.innerText = "Don't have an account?";
                toggleBtn.innerText = "Sign up";
            } else {
                pageTitle.innerText = "Create Account";
                pageSubtitle.innerText = "Join us today and start your journey.";
                toggleText.innerText = "Already have an account?";
                toggleBtn.innerText = "Log in";
            }
        });

        async function handleFormSubmit(event, endpoint) {
            event.preventDefault();
            const form = event.target;
            
            // --- reCAPTCHA Validation ---
            const captchaResponse = grecaptcha.getResponse(isLogin ? 0 : 1);
            if (!captchaResponse) {
                return tst("Please verify that you are not a robot!", "error");
            }

            const submitBtn = form.querySelector('button[type="submit"]');
            const btnSpan = submitBtn.querySelector('span');
            const formData = new FormData(form);
            const data = Object.fromEntries(formData.entries());
            
            // Send captcha response to backend
            data['g-recaptcha-response'] = captchaResponse;

            const originalText = btnSpan.innerText;
            submitBtn.disabled = true;
            btnSpan.innerHTML = `<span class="spinner"></span> Processing...`;

            try {
                const response = await fetch(endpoint, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(data)
                });

                const result = await response.json();

                if (response.ok && result.status) {
                    tst(result.message || "Action Successful!");
                    setTimeout(() => {
                        window.location.href = (endpoint === "/user/register") ? "/user/verify" : "/user/dashboard";
                    }, 1200);
                } else {
                    tst(result.message || "An error occurred", "error");
                    grecaptcha.reset(); // Reset on error so they can try again
                    if (result.rdr) location.href = result.rdr;
                }
            } catch (error) {
                tst("Server connection failed.", "error");
                grecaptcha.reset();
            } finally {
                submitBtn.disabled = false;
                btnSpan.innerText = originalText;
            }
        }

        loginForm.addEventListener('submit', (e) => handleFormSubmit(e, '/user/login'));
        registerForm.addEventListener('submit', (e) => handleFormSubmit(e, '/user/register'));
          
        async function handleCredentialResponse(response) {
          const res=  await fetch("/user/SignInWithGoogle",{
              method:"post",
              headers:{
                "Content-Type":"application/json"
              },
              body:JSON.stringify({idToken:response.credential})
            })
            const rs=await res.json()
            console.log(rs)
            if(rs.status){
              tst(rs.message)
              setTimeout(()=>{
                location.href="/user/dashboard"
              },2000)
              
            }
            else{
              tst(rs.message)
            }
            
            // Yahan se token backend ko bhejo verify karne ke liye
        }

        window.onload = function () {
            // 2. Initialize Google Identity
            google.accounts.id.initialize({
                client_id: "382846360562-rvchitsflgo2tnmqlk5o7ueps7p877kt.apps.googleusercontent.com", // <-- APNA ID YAHA DALO
                callback: handleCredentialResponse,
                use_fedcm_for_prompt: true, // Naye browser standard ke liye
                auto_select: false,
                itp_support:true// User ko choose karne do
            });

            // 3. One Tap Prompt dikhao
            google.accounts.id.prompt((notification) => {
                if (notification.isNotDisplayed()) {
                    console.warn("One Tap displayed nahi hua. Reason: " + notification.getNotDisplayedReason());
                }
            });

            // 4. Fallback Button (Extra safety ke liye)
            google.accounts.id.renderButton(
                document.getElementById("buttonDiv"),
                { theme: "outline", size: "large", shape: "pill" }
            );
        };
    </script>
</body>
</html>
